fragment CatalogSubject on Subject {
  id
  denomination
  courses {
    id
    participationCount
  }
  programs {
    id
    enrolledLearnersCount
  }
}

query Catalog {
  subjectsWithLinkedContent {
    ...CatalogSubject
  }
}