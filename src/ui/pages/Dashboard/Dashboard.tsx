import { Typography } from '@/ui/components';

const Dashboard = () => {
  return (
    <div>
      <Typography variant="h2" component="h1">
        This is an Dashboard route! - WIP
      </Typography>
      <Typography>This is the entry page for managing the content</Typography>
    </div>
  );
};

export default Dashboard;
