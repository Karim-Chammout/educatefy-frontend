fragment Instructor on Teacher {
  first_name
  followersCount
  courses {
    id
  }
  programs {
    id
  }
}

query Dashboard($instructorId: ID!) {
  instructor(id: $instructorId) {
    ...Instructor
  }
}
