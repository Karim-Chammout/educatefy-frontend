fragment ExploreSubject on Subject {
  id
  denomination
  courses {
    id
    participationCount
  }
  programs {
    id
    enrolledLearnersCount
  }
}

query Explore {
  subjectsWithLinkedContent {
    ...ExploreSubject
  }
}
