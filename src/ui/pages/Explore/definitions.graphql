fragment ExploreSubject on Subject {
  id
  denomination
  courses {
    id
    participationCount
  }
  programs {
    id
  }
}

query Explore {
  subjectsWithLinkedContent {
    ...ExploreSubject
  }
}
